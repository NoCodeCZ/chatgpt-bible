/**
 * Layout for dynamic pages
 * Add any common layout elements here (header, footer, etc.)
 */

export default function PagesLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}
